version: '3.5'
services:
  pymix:
    image: pymix
    restart: unless-stopped
    container_name: "pymix"
    #user: "501:20"
    networks:
      - subbox-bridge
    ports:
      - 8002:8002
    volumes:
      - ./db:/pymix/db
networks:
  subbox-bridge:
    external: true
    name: subbox-bridge
