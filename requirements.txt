anyio~=4.3.0
aiohttp~=3.9.5
fastapi~=0.110.1
uvicorn[standard]~=0.29.0
python-on-whales==0.64.2
dependency-injector
pyyaml~=6.0.1
pyrekordbox~=0.4.3
mutagen
music-tag
sqlalchemy~=2.0
psycopg2-binary~=2.9
alembic~=1.13
pyserato~=0.2
mediafile~=0.17.0
beets==2.13.1
jinja2~=3.1.4
pydantic~=2.7.0
# pydantic's EmailStr (invite-request capture) is a no-op without this — it raises on import.
email-validator~=2.2
pytest~=7.2.0
# Test-only: fastapi.testclient.TestClient is httpx-backed.
httpx~=0.27
starlette~=0.37.2
# The Prometheus exposition format and its process/platform collectors, for
# GET /metrics (pymix/services/metrics.py). Apache-2.0, no transitive deps.
prometheus-client~=0.20
watchfiles~=0.21.0
jsonschema
pytaglib==2.1.0
rapidfuzz~=3.9
yt-dlp>=2024.8.6
musicbrainzngs~=0.7.1
google-api-python-client~=2.100
google-auth~=2.30
# imported directly by google_sheets_service to build an explicitly-timed
# transport; both already arrive via google-api-python-client, pinned here
# because we import them rather than relying on them transitively.
google-auth-httplib2~=0.2
httplib2~=0.22